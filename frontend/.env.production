# Frontend Environment Variables for Production
# Production assumes a reverse proxy setup where:
# - Frontend static files served at /
# - Backend GraphQL API at /graphql
# - Backend REST API at /api/*
# All through the same domain/port (e.g., https://your-domain.com)
VITE_GRAPHQL_ENDPOINT=/graphql
VITE_FRONTEND_URL=https://your-domain.com
